- content_for :title, "#{@user.username}"

%p
  = "A Growstuff member since #{@user.created_at.strftime("%B %d, %Y")}."

%h3 Updates
%table
  - @user.updates.order("created_at desc").each do |update|
    %tr
      %td= link_to update.subject, update
      %td= update.created_at
