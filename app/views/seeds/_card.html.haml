.card.seed-card
  = link_to seed do
    = image_tag(seed_image_path(seed), alt: seed, class: 'img-card')
  .card-body
    .card-title
      = crop_icon(seed.crop)
      = link_to seed.crop, seed
  .card-footer
    = render 'members/tiny', member: seed.owner
    - if seed.tradable?
      .text-muted
        = icon 'fas', 'map'
        Will trade #{seed.tradable_to}
    .badge-location
      = icon 'fas', 'map-marker'
      = truncate(seed.owner.location, length: 25, separator: ' ', omission: '... ')