%p.text-muted
  From
  = link_to notification.sender, notification.sender
  on
  = notification.created_at

  - if notification.item.present?
    in response to:
    = link_to notification.item, notification.item

    = render notification.item

.well
  :growstuff_markdown
    #{ strip_tags(notification.body) }
