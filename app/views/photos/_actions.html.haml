.photo-actions
  - if can?(:edit, @photo) && can?(:destroy, @photo)
    %p
      - if can?(:edit, @photo)
        = edit_button(@photo)
      - if can?(:destroy, @photo)
        = delete_button(@photo)
