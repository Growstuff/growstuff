- if crop.approved? || can?(:wrangle, Crop)
  = link_to crop do
    %span.chip.crop-chip
      = crop_icon(crop)
      = crop.name
      - unless crop.approved?
        %badge.text-bg-warning= crop.approval_status
