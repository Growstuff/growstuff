.search-crops.text-center

  = form_tag(crops_search_path, :method => 'get', :class => 'form-inline', :role => 'form') do
    .banner-form-group
      = label_tag :search, t('.label')
      = text_field_tag :search, '', :class => 'form-control', :placeholder => t('.placeholder')
      = hidden_field_tag :exact_search, :value => true
      
  .hidden-xs.hidden-sm
    - cache cache_key_for(Crop, 'interesting'), :expires_in => 1.day do
      - Crop.first(4).each do |c|
        .col-md-3
          = render :partial => 'crops/thumbnail', :locals => { :crop => c }
