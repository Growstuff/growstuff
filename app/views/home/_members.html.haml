- cache cache_key_for(Member) do
  .hidden-xs
    - if Member.interesting.present?
      %section
      %h2= t('.title')

      .member-cards
        - Member.interesting.first(3).each do |m|
          = render partial: "members/thumbnail", locals: { member: m }

      %p.text-right
        = link_to "#{t('.view_all')} »", members_path
