- cache cache_key_for(Seed) do
  %h2.text-center= t('.seeds_available_to_trade')
  .homepage-cards
    - Seed.current.tradable.includes(:owner, :crop).order(created_at: :desc).limit(3).each do |seed|
      = render 'seeds/card', seed: seed
